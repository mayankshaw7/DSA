#include<iostream>
#include<string>
#include<vector>
using namespace std;

vector<vector<int>> permutation(vector<vector<int>>arr,int n,int map){
    if(arr.size()==0) {
        cout<<arr<<" ";
        return 
    }
}
int main(){
  int arr[]={1,2,3};
    int n=sizeof(arr)/sizeof(int);
    permutation(arr,"  ");
    return 0;
}